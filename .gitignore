.DS_Store
*.pdf
CLAUDE.md

# Estado de runtime del clúster / generador (IDs efímeros del Learner Lab)
.emr_state
.emr_state.bak
.ec2_state

# Secretos: API key de Gemini (NUNCA al repo). La carga run_agent.sh.
.env

# Datos TPC-DS generados (grandes; viven en S3, no en git).
# Se versiona solo la muestra sintética para verify_local.
data/tpcds/*
!data/tpcds/.gitkeep
!data/tpcds/sample

# Salidas de mediciones / trazas del agente
results/*
!results/.gitkeep

# Cachés de Python / uv
__pycache__/
*.pyc
.venv/

node_modules/
